---
id: T-0806
title: Drain lap 2: the four census and books PRs, whose real tail is their coverage declarations
state: open
epic: META
requested_by: owner
seen: false
effort: M
legacy_id: null
parent: null
opened: 2026-09-05
closed: null
pr: null
claimed_by: null
blocked_on: null
needs_bake: false
closed_at: null
claimed_run: null
---

**Re-cut 2026-09-05 17:10Z against the live backlog.** Four PRs whose conflicts are build
products PLUS a short, named tail of real research files. Bigger than lap 1 (T-0814) and
smaller than lap 3; each one needs a reader who can rule on the tail rather than pick a side.

| order | PR | ticket | behind | conflicts | the real tail |
|---|---|---|---|---|---|
| 1 | [#858](https://github.com/kevinrhaas/custom/pull/858) | T-0536 | 40 | 8 | `census_1840/README.md` |
| 2 | [#856](https://github.com/kevinrhaas/custom/pull/856) | T-0497, owner | 40 | 11 | `census_1840/` README + `coverage.json`; `identity_master.json`; `source_coverage.json` |
| 3 | [#850](https://github.com/kevinrhaas/custom/pull/850) | T-0559 | 44 | 9 | `census_1840/coverage.json`; one `.xlsx` |
| 4 | [#841](https://github.com/kevinrhaas/custom/pull/841) | T-0581 | 52 | 13 | `books/` README + `corpus.json` + `coverage.json` + `crosswalk.json`; `identity_master.json`; `source_coverage.json` |

**Half that tail is not a hand-merge either, and knowing which half is the whole skill of
this lap.** `identity_master.json` and `source_coverage.json` are BUILT by
`consolidate_resident_evidence.py --build`; the Newberry lead rulings and acquisition list
are built by `rule_newberry_leads.py --write`. Regenerate all of those — #880 had to, for
exactly this reason. What genuinely needs reading is the **coverage declarations**:
`census_1840/coverage.json` on three of these four, and `books/coverage.json` on #841. A
coverage note is a reading of what was read, so two branches rewriting it are making two
claims, and the merge must RULE with the ruling written into the note's own `why` — never
pick a side to clear a marker.

**#858, #856 and #850 all touch `census_1840/`, which is why they are adjacent.** Take them
in the order above so each one's coverage declaration is read against the last one's, not
against a base three merges stale.

**#841 is the one still gated on lap 3.** It filed T-0730, the site-budget question, which
the owner answered (T-0808 § 1) — and #836's fix has now LANDED on `dev`, so the wall it
was parked behind is gone. Verify the published tree is under budget with #841 applied
before merging; if it is not, that is a new fact and belongs in T-0803 (dev's own
budget-ceiling ticket), not in this PR.

---

**The table above is a SNAPSHOT and goes stale in under an hour** — the lane merged eight PRs unaided while these tickets were being written, and replaced two more with fresh PRs on the same tickets (#865 → #901, #835 → #902). **Re-derive this lap's membership with the rule in T-0814 before working it**, and close rather than merge anything a fresher PR on the same ticket has superseded.

**Acceptance:** all four merged into `dev` in the order above (squash). Every built file
regenerated by the tool that owns it. Every coverage declaration that both sides rewrote
is RULED, with the reasoning in the note's own `why`, and no marker survives.
`./tools/check.sh` exit 0 with `Queue OK`, `check-changelog.mjs` green. Open PRs against
`dev`: **4**.
