---
id: T-1400
title: The People view's tier filter and its transient, garrison and reconstructed pills
state: claimed
epic: META
requested_by: owner
seen: true
effort: S
legacy_id: null
parent: T-1394
opened: 2026-09-19
closed: null
pr: null
claimed_by: run 9/19/2026, 10:07:36 PM CT
blocked_on: null
needs_bake: false
closed_at: null
claimed_run: https://github.com/kevinrhaas/polecat-platform/actions/runs/35485582568
---

The People view's tier filter and its transient, garrison and reconstructed pills.

Piece 3 of 3 of **T-1394 — The resident layer's closeout: the rebuild order made executable and gated as a fixed point over every reader of the layer, one liberty entry per stage with its counts, the People view's tier filter and reconstructed pills, and the research doc's final tables by tier**, split because the parent needed more than one run's demonstration to be done. The parent keeps the full ask and its links; this ticket owns one slice of it.

**Acceptance:** (state it before working — one demonstration, never weakened to pass)

1. Every person in the compiled directory carries the programme stage that minted them
   (`stage`), read off the card's own committed `reconstruction.stage` and never
   re-derived from a record's shape; a person with no stage was read from a source, and
   the two sides partition the 3,228 rows exactly (1,285 read + 1,943 drawn).
2. The People view's filter panel carries a **Tier** row — `grade` out of the unlabelled
   three-question `facts` line, under the vocabulary's own name, each pill carrying the
   same sentence the card's attribute chips carry — and, below it, a **Reconstructed as**
   row whose pills are the programme's stages in the programme's order, with the
   programme's own title as the pill's tooltip: `read from a source`, women and children,
   trade households, modelled families, visitors, re-admitted, **garrison**,
   under-documented, named families.
3. `tools/smoke_renderer.mjs` asserts it at both viewports: the offer carries the stages,
   the garrison pill narrows the painted list to exactly `counts.by_stage.garrison`, the
   `read from a source` pill holds nobody graded `reconstructed`, and the row's pills sum
   to the whole directory.
4. `./tools/check.sh` green, including `compile_scene.py --all --check` over the
   regenerated sidecar.
