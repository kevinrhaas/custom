---
id: T-0145
title: The North parcel's roof pitches, and the ridge band that gates them
state: done
epic: TOWN
requested_by: loop
seen: true
effort: S
legacy_id: T-V1(b)
parent: T-0011
opened: 2026-08-22
closed: 2026-08-22
pr: 315
claimed_by: run 8/22/2026, 5:16:59 PM CT
blocked_on: null
needs_bake: false
---

The North parcel's roof pitches, and the ridge band that gates them.

Piece 2 of 2 of **T-0011 — The sixty North Division records get their sampled bands**, split because the parent needed more than one run's demonstration to be done. The parent keeps the full ask and its links; this ticket owns one slice of it.

**Acceptance:** the eleven North records whose `roof_pitch_deg` sits outside the pitch band their own
note cites are inside it — sampled from the family band like every other dimension — AND the ridge that
the sampled pitch and the sampled footprint together produce is gated against the crosswalk's committed
`ridge_ft` band, so the repair does not move the fault one field over. `recon_1835_north_w5_040`'s loft,
which its family's `levels` does not author, is settled in the same pass. Needs one bake.

Deferred deliberately out of T-0144 (see ROADMAP T-V1(b)): sampling pitch without gating the ridge would
put ridges outside a band their own note cites, which is the fault the parent parcel exists to fix.
