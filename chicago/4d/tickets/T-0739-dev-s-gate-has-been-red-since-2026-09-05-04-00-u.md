---
id: T-0739
title: dev's gate has been red since 2026-09-05 04:00 UTC: cohorts 13, 14 and 15 claim people who already carry a research row, the ladder re-derivations disagree, and data/sidecars/1835/people.json has drifted
state: done
epic: META
requested_by: loop
seen: false
effort: M
legacy_id: null
parent: null
opened: 2026-09-05
closed: 2026-09-05
pr: 863
claimed_by: run 9/5/2026, 4:34:50 AM CT
blocked_on: null
needs_bake: false
closed_at: 2026-09-05T09:35:56.926Z
claimed_run: null
---

dev's gate has been red since 2026-09-05 04:00 UTC: cohorts 13, 14 and 15 claim people who already carry a research row, the ladder re-derivations disagree, and data/sidecars/1835/people.json has drifted.

**Acceptance:** (state it before working — the definition of done, never weakened to pass)

**Acceptance:** (state it before working — one demonstration, never weakened to pass)

Found by T-0543, which ran `tools/check.sh` on a clean `origin/dev` worktree to answer the
question every branch has to answer — *is this red mine, or did I inherit it?* — and got NINE
failing steps that have nothing to do with its own diff. The same nine fail on `origin/dev` at
`1d7a32730` and at `85d650116`, byte for byte, and `chicago-4d-check.yml` has recorded
`failure` on every push to `dev` since 2026-09-05T04:00Z.

The nine, as `check.sh` names them:

- `sidecars derived from data/` — `data/sidecars/1835/people.json` is not what the dataset
  compiles to. The step prints its own repair: `python3 tools/compile_scene.py --all`.
- `the 75-person real-resident research cohort is fixed`, and the second and third of them
- `the thirteenth research cohort is fixed` — 76 ids
- `the fourteenth research cohort is fixed` — 76 ids
- `the fifteenth research cohort is fixed` — 76 ids
  (each reports *"cohort N claims people who already carry a research row"*, which is the
  cohorts' reservations colliding with rows written since they were fixed)
- `the civic, church, press and book residents re-derive from the ladder`
- `the regraded residents re-derive from the ladder too`

**Acceptance:** `tools/check.sh` exits 0 on `dev`, and each of the nine is repaired rather than
silenced — a cohort whose reservation genuinely collides is re-cut or the collision is recorded
as intended with the rule that permits it, and the two derived files are regenerated by their own
generators in the same commit. No step is deleted, weakened or made advisory to get the exit code.

This blocks every run in this lane: the dev gate is `check.sh` and nothing else
(`docs/PIPELINE.md`), so no PR can go green on its own merits until this is fixed, and it also
blocks the owner's promote-to-prod. It is filed rather than fixed here because T-0543 was one
unit of work about a census pairing and repairing the resident layer is a different unit.

