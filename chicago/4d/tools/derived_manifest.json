{
  "schema": 1,
  "_doc": "THE DERIVED RESEARCH LAYER, AND THE COMMAND THAT REBUILDS EACH PART OF IT. Read by tools/rederive.mjs, which .github/steward/pr-lap.sh calls when a merge conflicts. WHY THIS EXISTS (owner, 2026-09-10): with the lap finally able to merge at all (PR #1058), every remaining open PR was left alone for a REAL CONFLICT, and every conflicting file was a tool output two runs had each re-derived — directory crosswalks, the land-sale spend, identity_master. pr-lap.sh already draws exactly the right line for this: 'A conflict here is never resolved by hand: the merge takes either side to clear the marker and the tool then rewrites the file from source.' It just drew it around five bookkeeping files. This file widens it, EXPLICITLY, so the widening is auditable rather than a glob nobody can audit.",
  "_the_rule": "A path under `resolves` may have its conflict cleared by taking either side, because a step below rewrites it from its inputs. A path NOT listed here is refused exactly as before — that is the whole safety property, and it is why this file enumerates rather than pattern-matches. `resolves: []` means the step runs to keep the tree consistent but resolves no conflict: its outputs are still content a run may have meant to change.",
  "_the_backstop": "Nothing here is trusted on its say-so. After the sequence runs, tools/check.sh must pass, and it is check.sh that asserts each of these files re-derives from its inputs. A wrong entry here makes the gate RED, and pr-lap.sh does not push a red tree. The failure mode is a PR that stays open — the one it was already in — never a bad merge onto dev.",
  "_only_gated_tools": "A step may only appear here if tools/check.sh runs its tool with --check. That invariant is enforced by rederive.mjs --check, and it is not decoration — it is the whole reason the backstop above is real. Learned by nearly shipping the opposite: crosswalk_norris_1844_advertiser.py was in this file on first draft, and running it rebuilt norris_1844_advertiser_crosswalk_1835.json from 443 lines to 195, taking data/residents/directories.json down with it. check.sh did not invoke that tool AT ALL, so nothing in the gate would have gone red and the lap would have pushed the loss. An ungated derivation is one whose committed file nobody has ever asserted re-derives, and this file may not resolve a conflict by re-running one. READ THAT IN THE PAST TENSE: T-0867 gated the advertising crosswalk for the same reason, and it is listed below now. The example stands as the reason for the rule, not as a live claim about that tool — check what check.sh runs today, never what this sentence remembers.",
  "_must_actually_write": "A step's command must WRITE the files it claims. Obvious, and it is the one thing --check cannot see by reading, so `rederive.mjs --prove` runs every command and asserts each `resolves` file's mtime advanced. It exists because read_newberry_index.py was listed here for `data/research/newberry_index/leads.json` and, invoked bare, PRINTS ITS USAGE AND EXITS 0 — it writes nothing, and rebuilding leads.json needs --parse over OCR shards that live outside the repo. The bad entry survived a --check (the tool is gated), an idempotency test (nothing changed, because nothing ran) and a full-sequence run. It failed only when a real merge needed it, as `leads.json does not re-derive from its inputs`. The idempotency test that missed it used `timeout 400` with output suppressed, so a killed tool and a no-op tool looked identical. Run --prove when this file changes; a tool that needs inputs from outside the repo does not belong here at all.",
  "_must_reproduce": "AND A GATED TOOL THAT PASSES --check MAY STILL NOT BELONG HERE, which is the rule `_only_gated_tools` does not cover and the one that nearly admitted two more files on 2026-09-14. `--check` asks whether the committed OUTCOMES still follow from the inputs; a manifest entry resolves a conflict by re-running the BUILD and keeping whatever it writes. Those are different questions and they can disagree. Measured on a clean `dev` that afternoon: `crosswalk_census_1840_heads.py --check` reported `OK: 965 named 1840 head(s) — 13 matched, 18 candidate, 934 refused` and exited 0, while `--build` on the SAME TREE rewrote 137 lines of census_1840/crosswalk.json and resident_crosswalk.json — rule prose and evidence lists that had drifted, picking up the 1844 directory and a fourth Sherman. Nothing is stale by the gate's measure and the file still does not rebuild to itself. Listing it would have made the lap push those 137 lines into EVERY pull request it lapped, as a silent content change no ticket asked for. So before adding a step: run its command on a clean base and require that git reports NOTHING moved. A green --check is necessary and it is not sufficient.",
  "_never": "A file declaring `hand_authored: true` is refused even if listed. data/research/land_sales/resident_rulings.json is the one such file today: a judgement is not a derivation, and re-running a tool over it would erase the argument. rederive.mjs --check fails if any listed path declares it.",
  "_the_second_pass": "A STEP THAT READS A FILE A LATER STEP REBUILDS IS ONE MODEL BEHIND WHEN THE SEQUENCE ENDS, AND `--run` TWICE DOES NOT FIX IT (T-1363). The sequence below is a measured order, not a graph — its own `_doc` says so — and this is the edge it cannot express. `reconstruct_residents_1835.py --stage attribute_fill_arrival` DRAWS from data/reconstruction/1835_town_model.json, and model_town_1835.py rebuilds that model from the population the stage itself writes into. So any run that moves the population leaves the arrival draws standing on the model as it was BEFORE the move, and `reconstruct_residents_1835.py --check` goes red on roughly 1,400 cards with 'the committed value does not re-derive from the programme'. Measured on #1495 (T-1172, 2026-09-19) three times over, and reproduced deterministically for T-1363 on 2026-09-21 by moving one weight in arrival_year_of_the_known_layer: one full pass left 1,434 red assertions, and the three commands below cleared every one of them and returned the tree byte-identical to dev. RE-ORDERING CANNOT FIX IT: the model is derived FROM the layer the arrival stage writes into, so moving the stage below the model trades the lag for the opposite lag. A declared second pass can, because it is the cycle walked once more from a settled model, and tools/check.sh is the proof that it settles — model, profile, tiers and residents all --check green afterwards, which is what convergence means here. Entries run AFTER every step, in the order written, and each must name a step above: the second pass re-runs the sequence's own commands and admits no tool the sequence has not already gated and proven. The first entry carries `reads_rebuilt` — the file whose rebuild is the lag — and --check asserts a later step really does resolve it, so an entry cannot claim a lag that is not there. The entries after it carry no `reads_rebuilt` because their reason is different and one step removed: they are the repairs the lagging reader's rewrite makes necessary, so they must follow it.",
  "second_pass": [
    {
      "command": [
        "python3",
        "tools/reconstruct_residents_1835.py",
        "--stage",
        "attribute_fill_arrival",
        "--build"
      ],
      "reads_rebuilt": [
        "chicago/4d/data/reconstruction/1835_town_model.json"
      ],
      "why": "THE LAGGING READER, and the whole reason this pass exists. The stage draws arrival_year and reason_for_coming from the town model's arrival_year_of_the_known_layer and birthplace_of_the_old_settlers_who_came_by_1835 tables; model_town_1835.py rebuilds that model further down the sequence, from a profile of the very cards this stage writes. Drawn once more from the settled model, the draws reproduce."
    },
    {
      "command": [
        "python3",
        "tools/migrate_attribute_tiers.py",
        "--build"
      ],
      "why": "Downstream of the re-draw above: the tier table is derived per-attribute from the resident layer, so it moves whenever a card does, and the re-draw moves ~1,400 of them."
    },
    {
      "command": [
        "python3",
        "tools/profile_population_1835.py",
        "--build"
      ],
      "why": "Also downstream of the re-draw, and LAST by measurement rather than by taste: this is the order that converged by hand on #1495 and again under T-1363's reproduction. It profiles the population the re-draw just moved; the model it feeds is then asserted unmoved by model_town_1835.py --check in the gate, which is the statement that the cycle has settled rather than merely turned."
    }
  ],
  "_a_writer_belongs_here": "IF YOU ADD A GATED WRITER, ADD IT HERE, AND THAT IS ENFORCED RATHER THAN ASKED. A tool check.sh runs with --check, and which can also --build or --write, produces DERIVED content by definition: if this file has never heard of it, rederive.mjs never runs it, pr-lap.sh leaves every open PR stale against it, and the gate goes red with no automated remedy and no clue in the message. Owner, 2026-09-18, after four separate hand-fixes in one night on exactly that — derive_resident_roles.py went red on FOUR merges, location_spend.py on the splitter change, profile_population_1835.py and model_town_1835.py the same night: \"add a note to things to say if you add a writer it should be in that manifest, right?\" A note is what this preamble already was, and it did not hold. tools/audit_manifest_coverage.mjs is the gate: every gated writer is listed here, or it is in that file's NOT_DERIVABLE list with the measurement that says why it cannot be. It found four MORE on the day it was written. Before listing anything, measure what `_must_reproduce` asks: run the build on a clean tree and confirm it changes no committed byte. A tool that does not reproduce belongs in the exemption list, never here.",
  "steps": [
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/audit_wright_watercourses.py",
        "--write"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/audit_wright_watercourses.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/build_survey_tracts.py",
        "--build"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/build_survey_tracts.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/census_1840_composition.py",
        "--build"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/census_1840_composition.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/compare_norris_1844_readings.py",
        "--build"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/compare_norris_1844_readings.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/concord_letter_list_1834_01_01.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/concord_letter_list_1834_01_01.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/concord_letter_list_1834_04_01.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/concord_letter_list_1834_04_01.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/derive_hay_limits.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/derive_hay_limits.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/derive_shore_1812.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/derive_shore_1812.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/exhaust_tax_1833.py",
        "--build"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/exhaust_tax_1833.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/measure_north_branch_banks.py",
        "--write"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/measure_north_branch_banks.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/newspaper_corpus.py",
        "--build"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/newspaper_corpus.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/old_settlers.py",
        "--build"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/old_settlers.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/read_census_1830.py",
        "--build"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/read_census_1830.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/read_fergus_1839.py",
        "--build"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/read_fergus_1839.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/read_fergus_1839_election.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/read_fergus_1839_election.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/read_fergus_1839_register.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/read_fergus_1839_register.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/read_fergus_1843.py",
        "--build"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/read_fergus_1843.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/read_fergus_1843_civic.py",
        "--build"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/read_fergus_1843_civic.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/read_kinzie_addition_numerals.py",
        "--write"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/read_kinzie_addition_numerals.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/read_kinzie_addition_streets.py",
        "--write"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/read_kinzie_addition_streets.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/read_kinzie_addition_water_lots.py",
        "--write"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/read_kinzie_addition_water_lots.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/read_kinzie_block_name.py",
        "--write"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/read_kinzie_block_name.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/read_michigan_st_tract.py",
        "--write"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/read_michigan_st_tract.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/read_norris_1844.py",
        "--build"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/read_norris_1844.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/read_norris_1844_advertiser.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/read_norris_1844_advertiser.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/read_north_branch_bank_wash.py",
        "--build"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/read_north_branch_bank_wash.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/read_north_side_slough_na.py",
        "--write"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/read_north_side_slough_na.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/read_school_section_numerals.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/read_school_section_numerals.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/read_st_marys_baptisms.py",
        "--build"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/read_st_marys_baptisms.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/read_voter_lists.py",
        "--build"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/read_voter_lists.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/read_wabansia_block_numerals.py",
        "--write"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/read_wabansia_block_numerals.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/read_wabansia_streets.py",
        "--write"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/read_wabansia_streets.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/read_wabansia_water_lots.py",
        "--write"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/read_wabansia_water_lots.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/read_washington_madison_numerals.py",
        "--write"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/read_washington_madison_numerals.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/read_west_division_numerals.py",
        "--write"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/read_west_division_numerals.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/read_wright_legend_swatches.py",
        "--build"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/read_wright_legend_swatches.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/research_domains.py",
        "--build"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/research_domains.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/resolve_land_tracts.py",
        "--build"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/resolve_land_tracts.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/resolve_place_vocabulary.py",
        "--write"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/resolve_place_vocabulary.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/school_section_sale.py",
        "--build"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/school_section_sale.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/sort_land_sales_onto_tracts.py",
        "--build"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/sort_land_sales_onto_tracts.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/trace_north_branch.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/trace_north_branch.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/trace_river.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/trace_river.py",
        "--check"
      ]
    },
    {
      "why": "A SOURCE READ OUT OF ITS COMMITTED ORIGINAL. Nothing in the layer above it exists until this has run, so it sits with the other readers at the head of the manifest. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/trace_south_branch.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/trace_south_branch.py",
        "--check"
      ]
    },
    {
      "why": "The directory crosswalks: each reads its own transcribed claims file and adjudicates it against the residents layer. Two runs that both touch the residents layer both re-derive all of these, which is why they conflict so readily.",
      "command": [
        "python3",
        "tools/crosswalk_fergus_1839.py"
      ],
      "resolves": [
        "chicago/4d/data/research/directories/fergus_1839_crosswalk_1835.json"
      ]
    },
    {
      "command": [
        "python3",
        "tools/crosswalk_fergus_1839_election.py"
      ],
      "resolves": [
        "chicago/4d/data/research/directories/fergus_1839_election_crosswalk_1835.json"
      ]
    },
    {
      "command": [
        "python3",
        "tools/crosswalk_fergus_1839_lots.py"
      ],
      "resolves": [
        "chicago/4d/data/research/directories/fergus_1839_lots_crosswalk_1835.json"
      ]
    },
    {
      "command": [
        "python3",
        "tools/crosswalk_fergus_1839_register.py"
      ],
      "resolves": [
        "chicago/4d/data/research/directories/fergus_1839_register_crosswalk_1835.json"
      ]
    },
    {
      "command": [
        "python3",
        "tools/crosswalk_fergus_1843.py"
      ],
      "resolves": [
        "chicago/4d/data/research/directories/fergus_1843_crosswalk_1835.json"
      ]
    },
    {
      "command": [
        "python3",
        "tools/crosswalk_norris_1844.py"
      ],
      "resolves": [
        "chicago/4d/data/research/directories/norris_1844_crosswalk_1835.json"
      ]
    },
    {
      "why": "The ADVERTISING directory's crosswalk, the fourth of the four. It is the tool this file's `_only_gated_tools` note names as the near-miss, and it is listed now because the thing that disqualified it has been fixed: T-0867 added `step \"…and the advertising directory's crosswalk re-derives too\" python3 tools/crosswalk_norris_1844_advertiser.py --check` to check.sh, for the same reason the note gives — an ungated derivation stops describing the town it describes. The rule did not change; the tool crossed it. Verified byte-identical on a clean `dev` before listing (2026-09-14) — see `_must_reproduce`, which is the check the first draft never ran and which a green --check does not stand in for.",
      "command": [
        "python3",
        "tools/crosswalk_norris_1844_advertiser.py"
      ],
      "resolves": [
        "chicago/4d/data/research/directories/norris_1844_advertiser_crosswalk_1835.json"
      ]
    },
    {
      "why": "The ADDRESS back-projection (T-0633), the business-face half of the grammar whose residence half is the last step in this file. Same argument, same omission: the file says `generated_by` and nothing listed it, so the lap refused as a REAL CONFLICT every PR that had re-derived it — #1268 among them, where it conflicted alongside seven other tool outputs and not one disagreement about the town. Gated at check.sh by `back_project_addresses.py --check` AND a `--self-test`, and verified byte-identical on a clean `dev` before listing.",
      "command": [
        "python3",
        "tools/back_project_addresses.py",
        "--write"
      ],
      "resolves": [
        "chicago/4d/data/research/directories/address_back_projection.json"
      ]
    },
    {
      "why": "The church rolls, rebuilt from their transcribed records. T-0962 found second_presbyterian's 83 matched rows this morning; the count moves whenever the town gains a card the roll reaches, which is exactly the kind of drift two slices produce.",
      "command": [
        "python3",
        "tools/read_second_presbyterian.py",
        "--build"
      ],
      "resolves": [
        "chicago/4d/data/research/church/second_presbyterian_crosswalk.json"
      ]
    },
    {
      "why": "The Second Presbyterian roll's spend, after the crosswalk it spends.",
      "command": [
        "python3",
        "tools/spend_second_presbyterian_roll.py"
      ],
      "resolves": [
        "chicago/4d/data/research/church/second_presbyterian_roll_spend_1835.json"
      ],
      "verify": [
        "python3",
        "tools/spend_second_presbyterian_roll.py",
        "--check"
      ]
    },
    {
      "command": [
        "python3",
        "tools/read_st_cyr_register.py",
        "--build"
      ],
      "resolves": [
        "chicago/4d/data/research/church/st_cyr_crosswalk.json",
        "chicago/4d/data/research/church/crosswalk.json",
        "chicago/4d/data/research/church/coverage.json"
      ]
    },
    {
      "why": "The land-sale crosswalk folds resident_rulings.json — HAND-AUTHORED, and refused above — onto the deposit. The crosswalk is a derivation of the two; the rulings are not.",
      "command": [
        "python3",
        "tools/read_land_sales.py",
        "--build"
      ],
      "resolves": [
        "chicago/4d/data/research/land_sales/resident_crosswalk.json"
      ],
      "verify": [
        "python3",
        "tools/read_land_sales.py",
        "--check"
      ]
    },
    {
      "why": "The spends run AFTER their crosswalks, because a spend reads the crosswalk it is spending.",
      "command": [
        "python3",
        "tools/spend_directories.py"
      ],
      "resolves": [
        "chicago/4d/data/research/directories/spend_crosswalk_1835.json",
        "chicago/4d/data/residents/directories.json"
      ]
    },
    {
      "why": "Norris's 1844 businesses, dated back to the window.",
      "command": [
        "python3",
        "tools/date_norris_1844_businesses.py"
      ],
      "resolves": [
        "chicago/4d/data/research/directories/norris_1844_businesses_1835.json"
      ],
      "verify": [
        "python3",
        "tools/date_norris_1844_businesses.py",
        "--check"
      ]
    },
    {
      "why": "The later Fergus lists' spend, after the directory crosswalks it reads.",
      "command": [
        "python3",
        "tools/spend_fergus_1839_later_lists.py"
      ],
      "resolves": [
        "chicago/4d/data/research/directories/fergus_1839_later_spend_1835.json"
      ],
      "verify": [
        "python3",
        "tools/spend_fergus_1839_later_lists.py",
        "--check"
      ]
    },
    {
      "command": [
        "python3",
        "tools/spend_land_sales.py"
      ],
      "resolves": [
        "chicago/4d/data/research/land_sales/resident_spend_1835.json"
      ],
      "verify": [
        "python3",
        "tools/spend_land_sales.py",
        "--check"
      ]
    },
    {
      "why": "The obituary crosswalk: the old-settlers death notices adjudicated against the residents layer, on the same footing as the directory crosswalks above.",
      "command": [
        "python3",
        "tools/read_fergus_obits.py",
        "--build"
      ],
      "resolves": [
        "chicago/4d/data/research/old_settlers/death_notices_crosswalk_1835.json"
      ],
      "verify": [
        "python3",
        "tools/read_fergus_obits.py",
        "--check"
      ]
    },
    {
      "why": "The 1835 business register, compiled from the newspaper corpus.",
      "command": [
        "python3",
        "tools/compile_register.py",
        "--build"
      ],
      "resolves": [
        "chicago/4d/data/research/newspapers/register_1835.json"
      ],
      "verify": [
        "python3",
        "tools/compile_register.py",
        "--check"
      ]
    },
    {
      "why": "The cross-domain consolidation reads every crosswalk above, so it runs after all of them.",
      "command": [
        "python3",
        "tools/consolidate_resident_evidence.py",
        "--build"
      ],
      "resolves": [
        "chicago/4d/data/research/residents/identity_master.json",
        "chicago/4d/data/research/residents/source_coverage.json",
        "chicago/4d/data/research/residents/grading_proposal.json",
        "chicago/4d/data/research/residents/ladder_coverage.json"
      ],
      "verify": [
        "python3",
        "tools/consolidate_resident_evidence.py",
        "--check"
      ]
    },
    {
      "why": "T-1138's PR sat DIRTY for five hours on this file. lead_crosswalk.json is a recomputed summary of the residents layer — counts and a fingerprint, not a list two branches can append to — so a merge leaves two disagreeing totals (1010 leads on 1418 cards against 1008 on 1416) and neither side is right: the merged corpus answers 1009 on 1417. The lap could not heal it because the step was not here, so every lap left the conflict exactly where it found it. It costs 0 s measured, which is the whole argument for adding it.\nNOT ADDED BESIDE IT, DELIBERATELY: tools/read_newberry_index.py --parse, which rebuilds leads.json itself. Measured on 2026-09-15 it is 132 s for ONE of its four volumes — about nine minutes for the set, against a lap that runs in 23-37 s for a whole queue of PRs. A lap that took nine minutes per branch would be the gating problem this manifest was built to end. When leads.json itself goes stale (the volumes, the WORKS table or the card text moved, and read_newberry_index.py --check says which) that parse is a run's own work, not the lap's.",
      "command": [
        "python3",
        "tools/rule_newberry_leads.py",
        "--write"
      ],
      "resolves": [
        "chicago/4d/data/research/newberry_index/lead_crosswalk.json"
      ]
    },
    {
      "why": "Cohort 14's ruled ledger. It reads the four directory crosswalks and T-0816's rulings, so every stretch of T-0987 and every re-derivation of a crosswalk moves it — and nothing regenerated it, because it was not here. That is T-1109's whole subject: PR #1341's gate went red on `pass_14_findings.json does not match a re-derivation` for refusals the crosswalks had gained (fergus_1839, fergus_1843, norris_1844 appearing in `refusals_seen`), with a CLEAN merge and nothing to flag it — the #1333 shape, where the base changes an input the branch never touched. It is admissible now because #1341 is what made it so: that PR adds `complete_resident_research_pass_14.py --check` to check.sh, which is the gate `_only_gated_tools` requires. Verified idempotent before listing, per `_must_reproduce`. IT ALSO WRITES T-0509's WORKING WORKBOOK (line 594), and that path was missing from this list — which is what left six pull requests stranded on 2026-09-17. The workbook is a zip and every run gives it fresh member timestamps, so two branches that both ran this step hold different bytes for the same content and the merge conflicts every time. The lap asks `rederive --resolvable` before it clears anything; an unlisted path is refused BY DESIGN, so the lap abandoned the whole pull request — nine of its refusals that afternoon named this one file. Listing it is the fix, and it is honest: the step rewrites it from source on every run.",
      "command": [
        "python3",
        "tools/complete_resident_research_pass_14.py"
      ],
      "resolves": [
        "chicago/4d/data/research/residents/pass_14_findings.json",
        "chicago/reference/resident-research/T-0509/T-0509_resident_research_working.xlsx"
      ],
      "verify": [
        "python3",
        "tools/complete_resident_research_pass_14.py",
        "--check"
      ]
    },
    {
      "why": "A READING SPENT ONTO THE LAYER: it needs its source read and it must precede anything that counts what it wrote. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/spend_census_1840_heads.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/spend_census_1840_heads.py",
        "--check"
      ]
    },
    {
      "why": "A READING SPENT ONTO THE LAYER: it needs its source read and it must precede anything that counts what it wrote. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/spend_civic_roll_bounds.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/spend_civic_roll_bounds.py",
        "--check"
      ]
    },
    {
      "why": "A READING SPENT ONTO THE LAYER: it needs its source read and it must precede anything that counts what it wrote. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/spend_civic_voter_lists.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/spend_civic_voter_lists.py",
        "--check"
      ]
    },
    {
      "why": "A READING SPENT ONTO THE LAYER: it needs its source read and it must precede anything that counts what it wrote. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/spend_fergus_1839_lot_sale.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/spend_fergus_1839_lot_sale.py",
        "--check"
      ]
    },
    {
      "why": "A READING SPENT ONTO THE LAYER: it needs its source read and it must precede anything that counts what it wrote. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/spend_land_sales_rulings.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/spend_land_sales_rulings.py",
        "--check"
      ]
    },
    {
      "why": "REBUILD ONLY, resolves nothing — and the ordering here was learned the hard way on PR #1055. mint_civic_residents writes 486 resident CARDS from the ladder, and consolidate moves the inputs it reads: built before consolidate it still differed on two cards, and only converged when re-run after it. Its outputs are resident cards, which a run may have meant to change by hand, so a conflict in one is never cleared by this file.",
      "command": [
        "python3",
        "tools/mint_civic_residents.py",
        "--build"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/mint_civic_residents.py",
        "--check"
      ]
    },
    {
      "why": "The duplicate-cluster ledger, rebuilt from the town and the merge rulings — it runs after mint because it reads the cards mint writes. `--candidates` and NOT `--apply`: --apply performs the card merges and rewrites resident cards (tested on a clean dev, it modified hh_kinzie_john_h.json), which is a judgement being carried out and never something a merge resolution may do on its own. --candidates writes this one ledger and nothing else. Added after the first real use of this manifest left check.sh red on `town_card_candidates.json does not re-derive`.",
      "command": [
        "python3",
        "tools/consolidate_town_cards.py",
        "--candidates"
      ],
      "resolves": [
        "chicago/4d/data/research/residents/town_card_candidates.json"
      ],
      "verify": [
        "python3",
        "tools/consolidate_town_cards.py",
        "--check"
      ]
    },
    {
      "why": "data/residents/index.json — the town's index and its `merged` redirect table, rebuilt from the cards. The single most frequent blocker in the laps of 2026-09-10/11: it was named in nine of the five laps' refusals, more than any other file.",
      "command": [
        "python3",
        "tools/rebuild_resident_index.py",
        "--write"
      ],
      "resolves": [
        "chicago/4d/data/residents/index.json"
      ],
      "verify": [
        "python3",
        "tools/rebuild_resident_index.py",
        "--check"
      ]
    },
    {
      "why": "T-1313 seats the family members the sources NAME (T-1170's first half): it writes household cards and re-indexes them, so it runs after the index is built and before everything downstream that COUNTS people — the ladder, the roles, the profile, the town model, the order book, the census and the ledger each hold their own copy of the town's totals. Listed here on the day it landed rather than later: the coverage audit's write-mode pattern still misses an argparse-declared --build (T-1302), so this writer slipped past the gate exactly as export_borderline_roster.py did on #1422, and leaving it out is what made a full re-derivation need two passes by hand.",
      "command": [
        "python3",
        "tools/spend_stated_families.py",
        "--build"
      ],
      "resolves": [
        "chicago/4d/docs/RESEARCH/stated-families-2026-09.md"
      ],
      "verify": [
        "python3",
        "tools/spend_stated_families.py",
        "--check"
      ]
    },
    {
      "why": "T-1320 reads the BOOK corpus for kinship and writes kin rows onto household cards, so it belongs beside the other card writers: after the index is rebuilt and before anything downstream counts the town. It resolves no conflict — a household card is written by several passes and re-running this one to clear a conflict would take another pass's writes down with it — so `resolves` is empty and the step runs only to keep the tree consistent. IT REPRODUCES: the build appends a note only when the sentence is not already there and sorts `sources` and `book_kin`, so `--build` on the committed tree writes the same bytes back, measured twice on a clean checkout before this entry was added.",
      "command": [
        "python3",
        "tools/spend_book_kin.py",
        "--build"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/spend_book_kin.py",
        "--check"
      ]
    },
    {
      "why": "A RECONSTRUCTION STAGE WRITES CARDS, so it belongs where the other card writers are: after the index is rebuilt and before anything counts the town. Listed 2026-09-18 because outside the manifest there is NO hand order that converges — measured on #1452: build the stage, all three `--check`s green, run the 50-step manifest, and two of the three are red again, because the manifest's own card writers move their inputs. Run them after instead and every counter downstream is stale. That is a two-cycle, not a step somebody forgot. T-1169's arrival fill: arrival year, origin and reason on 1,258 households.",
      "command": [
        "python3",
        "tools/reconstruct_residents_1835.py",
        "--stage",
        "attribute_fill_arrival",
        "--build"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/reconstruct_residents_1835.py",
        "--check"
      ]
    },
    {
      "why": "A RECONSTRUCTION STAGE WRITES CARDS, so it belongs where the other card writers are: after the index is rebuilt and before anything counts the town. Listed 2026-09-18 because outside the manifest there is NO hand order that converges — measured on #1452: build the stage, all three `--check`s green, run the 50-step manifest, and two of the three are red again, because the manifest's own card writers move their inputs. Run them after instead and every counter downstream is stale. That is a two-cycle, not a step somebody forgot. T-1304's drawn sex and age bands, each seeded by the person's own id.",
      "command": [
        "python3",
        "tools/reconstruct_sex_age.py",
        "--build"
      ],
      "resolves": [
        "chicago/4d/data/reconstruction/1835_sex_age_model.json"
      ],
      "verify": [
        "python3",
        "tools/reconstruct_sex_age.py",
        "--check"
      ]
    },
    {
      "why": "A RECONSTRUCTION STAGE WRITES CARDS, so it belongs where the other card writers are: after the index is rebuilt and before anything counts the town. Listed 2026-09-18 because outside the manifest there is NO hand order that converges — measured on #1452: build the stage, all three `--check`s green, run the 50-step manifest, and two of the three are red again, because the manifest's own card writers move their inputs. Run them after instead and every counter downstream is stale. That is a two-cycle, not a step somebody forgot. T-1303's read sex and age, spent off the forename table and the rolls.",
      "command": [
        "python3",
        "tools/spend_person_sex_age.py"
      ],
      "resolves": [
        "chicago/4d/data/research/residents/sex_age_spend.json"
      ],
      "verify": [
        "python3",
        "tools/spend_person_sex_age.py",
        "--check"
      ]
    },
    {
      "why": "A RECONSTRUCTION STAGE WRITES CARDS, so it belongs where the other card writers are: after the index is rebuilt and before anything counts the town. Listed 2026-09-18 with T-1171, for the reason the three stages above it were listed on #1452 — outside the manifest there is no hand order that converges. Stage `modelled_families`: the kin core the household model says each head the sources leave standing alone kept, drawn against the order book's quota.",
      "command": [
        "python3",
        "tools/reconstruct_modelled_families.py",
        "--build"
      ],
      "resolves": [
        "chicago/4d/data/reconstruction/1835_modelled_families.json"
      ],
      "verify": [
        "python3",
        "tools/reconstruct_modelled_families.py",
        "--check"
      ]
    },
    {
      "why": "THE STAGE AFTER IT, AND IT HAS TO RUN AFTER IT. Listed 2026-09-19 with T-1174. Stage `women_and_children` draws against what the order book still wants, and `modelled_families` above fills the same 24 family buckets first — run in the other order the second stage would draw into quota the first one is about to take. It writes the female-headed households the naming sources never produced and the 556 people in them; measured 2026-09-19, a repeat `--build` on a settled tree changed no committed byte.",
      "command": [
        "python3",
        "tools/reconstruct_women_children.py",
        "--build"
      ],
      "resolves": [
        "chicago/4d/data/reconstruction/1835_women_children.json"
      ],
      "verify": [
        "python3",
        "tools/reconstruct_women_children.py",
        "--check"
      ]
    },
    {
      "why": "THE TRADE HOUSEHOLDS THE ORDER BOOK ORDERS (T-1347, of T-1173). It reads the order book's twenty-four `family/trade` buckets, the Fergus 1839 trade table, the town model's census comparison and size histogram and the invented name pools, and writes 308 reconstructed heads into data/residents/reconstructed_trades/ plus its own ledger. It FOLLOWS the order book because the book is its quota and it carries its own fills back into it, and it follows the two stages above because all three draw against the same book. It PRECEDES compile_scene.py because the scene overlays these cards onto the People directory. PLACED BY LAYER, as the steps around it are, and not by a measured dependency: the honest test is the first time a change to the order book or the trade table fails to propagate here. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS — `resolves` stays empty for the same reason it is empty on the steps beside it.",
      "command": [
        "python3",
        "tools/reconstruct_trade_households.py",
        "--build"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/reconstruct_trade_households.py",
        "--check"
      ]
    },
    {
      "why": "THE STAGE AFTER THAT ONE, and the only one that draws from no town-model share at all. Listed 2026-09-19 with T-1349. Stage `garrison` returns the fort, which the order book refuses to apportion — the Act of 2 March 1821 § 2 for what a company of infantry consisted of, Andreas for how many stood at the post, the Army's 1835 General Regulations for the recruiting window every age is drawn inside. It runs AFTER `women_and_children` because its names must step past that stage's and not the other way round: run in the other order, 43 of the female-headed cards redraw. Measured 2026-09-19, a repeat `--build` on a settled tree changed no committed byte.",
      "command": [
        "python3",
        "tools/reconstruct_garrison_1835.py",
        "--build"
      ],
      "resolves": [
        "chicago/4d/data/reconstruction/1835_garrison.json"
      ],
      "verify": [
        "python3",
        "tools/reconstruct_garrison_1835.py",
        "--check"
      ]
    },
    {
      "why": "The resident-research synthesis, downstream of the crosswalks and read by the ladder.",
      "command": [
        "python3",
        "tools/synthesize_resident_research.py"
      ],
      "resolves": [
        "chicago/4d/data/research/residents/synthesis_2026_09_02.json",
        "chicago/4d/docs/RESEARCH/resident-household-synthesis-2026-09-02.md"
      ],
      "verify": [
        "python3",
        "tools/synthesize_resident_research.py",
        "--check"
      ]
    },
    {
      "why": "THE RESIDENT AND BUSINESS LAYER — mints, carries and the passes that write onto a card. After the spends that feed them, before everything that counts the town. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/apply_census_1840_bridges.py",
        "--write"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/apply_census_1840_bridges.py",
        "--check"
      ]
    },
    {
      "why": "THE RESIDENT AND BUSINESS LAYER — mints, carries and the passes that write onto a card. After the spends that feed them, before everything that counts the town. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/associations.py",
        "--write"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/associations.py",
        "--check"
      ]
    },
    {
      "why": "THE RESIDENT AND BUSINESS LAYER — mints, carries and the passes that write onto a card. After the spends that feed them, before everything that counts the town. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/audit_scene_window_trades.py",
        "--write"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/audit_scene_window_trades.py",
        "--check"
      ]
    },
    {
      "why": "THE RESIDENT AND BUSINESS LAYER — mints, carries and the passes that write onto a card. After the spends that feed them, before everything that counts the town. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/carry_kinzie_west.py",
        "--write"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/carry_kinzie_west.py",
        "--check"
      ]
    },
    {
      "why": "THE RESIDENT AND BUSINESS LAYER — mints, carries and the passes that write onto a card. After the spends that feed them, before everything that counts the town. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/derive_presence_evidence_leg.py",
        "--write"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/derive_presence_evidence_leg.py",
        "--check"
      ]
    },
    {
      "why": "THE RESIDENT AND BUSINESS LAYER — mints, carries and the passes that write onto a card. After the spends that feed them, before everything that counts the town. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/mint_documented_residents.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/mint_documented_residents.py",
        "--check"
      ]
    },
    {
      "why": "THE RESIDENT AND BUSINESS LAYER — mints, carries and the passes that write onto a card. After the spends that feed them, before everything that counts the town. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/mint_placed_residents.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/mint_placed_residents.py",
        "--check"
      ]
    },
    {
      "why": "THE RESIDENT AND BUSINESS LAYER — mints, carries and the passes that write onto a card. After the spends that feed them, before everything that counts the town. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/normalise_structure_function.py",
        "--build"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/normalise_structure_function.py",
        "--check"
      ]
    },
    {
      "why": "THE RESIDENT AND BUSINESS LAYER — mints, carries and the passes that write onto a card. After the spends that feed them, before everything that counts the town. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/register_letter_list_suspicions.py",
        "--build"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/register_letter_list_suspicions.py",
        "--check"
      ]
    },
    {
      "why": "THE RESIDENT AND BUSINESS LAYER — mints, carries and the passes that write onto a card. After the spends that feed them, before everything that counts the town. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/trade_census_1835.py",
        "--build"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/trade_census_1835.py",
        "--check"
      ]
    },
    {
      "why": "THE RESIDENT AND BUSINESS LAYER — mints, carries and the passes that write onto a card. After the spends that feed them, before everything that counts the town. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/trade_census_spend_1835.py",
        "--build"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/trade_census_spend_1835.py",
        "--check"
      ]
    },
    {
      "why": "The ladder's spend, which reads the consolidated evidence above it.",
      "command": [
        "python3",
        "tools/spend_ladder_rungs.py",
        "--build"
      ],
      "resolves": [
        "chicago/4d/data/research/residents/ladder_spend.json"
      ],
      "verify": [
        "python3",
        "tools/spend_ladder_rungs.py",
        "--check"
      ]
    },
    {
      "why": "Writes the later-trade pointer onto a card, and T-0693's gate refuses a bare `none_recorded` beside a dated trade. MUST RUN BEFORE any writer that re-derives roles from the same card: both write hh_elston_daniel, and the other order leaves whichever ran first reporting red, which reads like a loop and is not (2026-09-17).",
      "command": [
        "python3",
        "tools/qualify_later_trades.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/qualify_later_trades.py",
        "--check"
      ]
    },
    {
      "why": "Writes the dated plural roles onto the person cards, and the 1835 occupation view derived from them (T-1229). MUST RUN AFTER qualify_later_trades.py, which the step above states from its own side: both write hh_elston_daniel and the other order leaves whichever ran first red. `resolves: []` ON PURPOSE — it writes ~1,000 files under data/residents/households/ and this manifest ENUMERATES rather than globs, so listing them is not auditable and listing none is honest: the step runs to keep the tree consistent and clears no conflict. A household-card conflict stays refused (T-1282). Added 2026-09-18 after this exact step went red on FOUR hand-merges in one night — it is gated by check.sh and was in no manifest, so rederive.mjs never ran it.",
      "command": [
        "python3",
        "tools/derive_resident_roles.py",
        "--write"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/derive_resident_roles.py",
        "--check"
      ]
    },
    {
      "why": "T-1375, from T-1177. The `community` of every person, derived off what the layer already says — a household's `origin` block, or a reconstructed person's own name-pool community — and graded no higher than the field it came from. PLACED AFTER derive_resident_roles.py AND BEFORE compile_scene.py because that is the order its inputs and its consumer impose: every card writer above it may change an `origin`, and the People view's rows and community filter are built out of this file by compile_scene. Its output is a single enumerated file with no other writer, so the manifest can own it outright and the lap settles a conflict there by rebuilding. A rebuild is also the ONLY correct resolution: two branches that both add residents produce two partial community tables, and merging them would leave a table that matches neither tree.",
      "command": [
        "python3",
        "tools/derive_person_community.py"
      ],
      "resolves": [
        "chicago/4d/data/residents/community.json"
      ],
      "verify": [
        "python3",
        "tools/derive_person_community.py",
        "--check"
      ]
    },
    {
      "why": "The business location spend — evidence reach apart from model seat, with four retentions a gate holds (T-1239). It keys on resident identities, so anything that moves a card moves its counts: it went red on the T-1155 splitter change and had to be rebuilt by hand because it was gated and unlisted.",
      "command": [
        "python3",
        "tools/location_spend.py",
        "--build"
      ],
      "resolves": [
        "chicago/4d/data/research/location_spend.json",
        "chicago/4d/docs/RESEARCH/location-spend.md"
      ],
      "verify": [
        "python3",
        "tools/location_spend.py",
        "--check"
      ]
    },
    {
      "why": "T-1144 acceptance 7: the per-person convergence coverage table and its report — which roles[] and which home/work/other locations reach 1 July 1835, joined from the roles, the location reconciliation and the business register. A gated writer, so it belongs here: it re-derives whenever any of those three layers moves, and a lap that rebuilt them without rebuilding this join would leave check.sh red with nothing to run.",
      "command": [
        "python3",
        "tools/report_convergence_coverage.py",
        "--build"
      ],
      "resolves": [
        "chicago/4d/data/research/convergence_coverage.json.gz",
        "chicago/4d/docs/RESEARCH/convergence-coverage-2026-09.md"
      ],
      "verify": [
        "python3",
        "tools/report_convergence_coverage.py",
        "--check",
        "--quiet"
      ]
    },
    {
      "why": "THE PRESENCE RULINGS (T-1386): the 827 people the research left `uncertain` ruled into the town, each at the tier its own dated readings reach. AFTER every writer that can change a card's `present_on_scene_date` or its evidence blocks — the mints, the spends, the carries and the evidence leg — because the ruling is derived from those blocks and from nothing else. BEFORE everything that counts the town: profile_population_1835.py, build_order_book_1835.py and town_census.py all read this file, and the gate screen reads the census. Placed by layer and reasoned from what it reads, like its neighbours; the honest test is the first time a card's evidence moves and this fails to follow, which `--check` turns into a red gate.",
      "command": [
        "python3",
        "tools/rule_presence_1835.py",
        "--build"
      ],
      "resolves": [
        "chicago/4d/data/reconstruction/1835_presence_rulings.json"
      ],
      "verify": [
        "python3",
        "tools/rule_presence_1835.py",
        "--check"
      ]
    },
    {
      "why": "The known population profiled on ten axes (T-1160). It also writes the age-band and reason vocabulary into data/residents/index.json, which rebuild_resident_index.py owns — measured 2026-09-18: running that rebuild AFTER this changes nothing, so the order between them is free, and index.json stays listed against the index step alone rather than twice. MUST RUN BEFORE model_town_1835.py, which reads that vocabulary.",
      "command": [
        "python3",
        "tools/profile_population_1835.py",
        "--build"
      ],
      "resolves": [
        "chicago/4d/data/reconstruction/1835_population_profile.json",
        "chicago/4d/docs/RESEARCH/1835_population_profile.md"
      ],
      "verify": [
        "python3",
        "tools/profile_population_1835.py",
        "--check"
      ]
    },
    {
      "why": "The 1835 town model — five sections, every figure a bounded range that says what it rests on (T-1293). Reads the profile's vocabulary, so it runs after it.",
      "command": [
        "python3",
        "tools/model_town_1835.py",
        "--build"
      ],
      "resolves": [
        "chicago/4d/data/reconstruction/1835_town_model.json",
        "chicago/4d/docs/RESEARCH/1835_town_model.md"
      ],
      "verify": [
        "python3",
        "tools/model_town_1835.py",
        "--check"
      ]
    },
    {
      "why": "One row per home, workplace and business-location claim, resolved as far as its evidence reaches and no further (T-1237). It keys on resident ids, so a change to the identity splitter moves its rows. ADDED 2026-09-18 by tools/audit_manifest_coverage.mjs, the gate that refuses a gated writer the manifest has never heard of. Reproduction measured on a clean dev tree before listing, per `_must_reproduce`: the rebuild changed no committed byte.",
      "command": [
        "python3",
        "tools/location_reconciliation.py",
        "--build"
      ],
      "resolves": [
        "chicago/4d/data/research/location_reconciliation.json.gz",
        "chicago/4d/docs/RESEARCH/location-reconciliation-2026-09-17.md"
      ],
      "verify": [
        "python3",
        "tools/location_reconciliation.py",
        "--check"
      ]
    },
    {
      "why": "The per-attribute tier table — attested, inferred or reconstructed, per field (T-1158). Derived from the resident layer, so it moves whenever a card does. ADDED 2026-09-18 by tools/audit_manifest_coverage.mjs, the gate that refuses a gated writer the manifest has never heard of. Reproduction measured on a clean dev tree before listing, per `_must_reproduce`: the rebuild changed no committed byte.",
      "command": [
        "python3",
        "tools/migrate_attribute_tiers.py",
        "--build"
      ],
      "resolves": [
        "chicago/4d/data/research/residents/attribute_tiers.json"
      ],
      "verify": [
        "python3",
        "tools/migrate_attribute_tiers.py",
        "--check"
      ]
    },
    {
      "why": "The Fergus 1839 population and Fort Dearborn lot readings, parsed from the committed transcription. ADDED 2026-09-18 by tools/audit_manifest_coverage.mjs, the gate that refuses a gated writer the manifest has never heard of. Reproduction measured on a clean dev tree before listing, per `_must_reproduce`: the rebuild changed no committed byte.",
      "command": [
        "python3",
        "tools/read_fergus_1839_lots.py",
        "--build"
      ],
      "resolves": [
        "chicago/4d/data/research/directories/claims/fergus_1839_population.json",
        "chicago/4d/data/research/directories/claims/fergus_1839_ft_dearborn_lots.json"
      ],
      "verify": [
        "python3",
        "tools/read_fergus_1839_lots.py",
        "--check"
      ]
    },
    {
      "why": "The survey of kin the records state, read off the resident layer. ADDED 2026-09-18 by tools/audit_manifest_coverage.mjs, the gate that refuses a gated writer the manifest has never heard of. Reproduction measured on a clean dev tree before listing, per `_must_reproduce`: the rebuild changed no committed byte.",
      "command": [
        "python3",
        "tools/survey_stated_kin.py",
        "--write"
      ],
      "resolves": [
        "chicago/4d/data/residents/kin_survey.json"
      ],
      "verify": [
        "python3",
        "tools/survey_stated_kin.py",
        "--check"
      ]
    },
    {
      "why": "The person-fact table (T-1232). It reached dev on 2026-09-17 and was in no lap, so every branch the janitor lapped that day went red on it — four of them — with the gate naming the one command that fixes it.",
      "command": [
        "python3",
        "tools/spend_person_facts.py"
      ],
      "resolves": [
        "chicago/4d/data/residents/person_facts.json"
      ],
      "verify": [
        "python3",
        "tools/spend_person_facts.py",
        "--check"
      ]
    },
    {
      "why": "THE TOWN COMPILED FROM THE LAYER: it reads finished cards and writes the town-level record, so it follows the layer and precedes the scene. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/adopt_street_faces.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/adopt_street_faces.py",
        "--check"
      ]
    },
    {
      "why": "THE TOWN COMPILED FROM THE LAYER: it reads finished cards and writes the town-level record, so it follows the layer and precedes the scene. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/build_order_book_1835.py",
        "--build"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/build_order_book_1835.py",
        "--check"
      ]
    },
    {
      "why": "THE 1835 BUSINESS STAFFING MODEL (T-1183) — the rule T-1189 staffs every business by. It reads the business layer, the 1839 trade table, the town model and the residents vocabulary, and writes a table of establishment kinds with the roles each employed plus a dry run of what the layer implies. It writes NO person, so rebuilding it can never touch a card; it is listed here because it is a gated writer and an unlisted gated writer leaves every open PR stale against it with no automated remedy. Placed after the order book, which is the other quota over these same committed layers; there is no derived dependency between them and this position is reasoned from what the tool reads and writes, not measured.",
      "command": [
        "python3",
        "tools/build_staffing_model_1835.py",
        "--build"
      ],
      "resolves": [
        "chicago/4d/data/reconstruction/1835_business_staffing_model.json",
        "chicago/4d/docs/RESEARCH/1835_business_staffing_model.md"
      ],
      "verify": [
        "python3",
        "tools/build_staffing_model_1835.py",
        "--check"
      ]
    },
    {
      "why": "THE COHORT THE ORDER BOOK CANNOT APPORTION (T-1352, piece 1 of T-1178). It reads the town model's July population, the 1843 census table, the land-sale register and its resident crosswalk, and bounds the people who were in the town on the scene date and not of it. It follows the order book because the row it fills is the one the book leaves open, and it must follow model_town_1835.py because the bracket's floor is a fraction of that model's own resident point. PLACED BY LAYER, as the 89 steps around it are, and not by a measured dependency: the honest test is the first time a change to the town model or the resident crosswalk fails to propagate here. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS — `resolves` stays empty for the same reason it is empty on the steps beside it.",
      "command": [
        "python3",
        "tools/model_transients_1835.py",
        "--build"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/model_transients_1835.py",
        "--check"
      ]
    },
    {
      "why": "THE TOWN COMPILED FROM THE LAYER: it reads finished cards and writes the town-level record, so it follows the layer and precedes the scene. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/compile_businesses.py",
        "--build"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/compile_businesses.py",
        "--check"
      ]
    },
    {
      "why": "The business layer read back against the research it is compiled from, so it follows the compile step above it. PLACED BY LAYER, like its neighbours: it reads data/businesses/ and the three research files, and writes only its own report.",
      "command": [
        "python3",
        "tools/audit_businesses.py",
        "--write"
      ],
      "resolves": [
        "chicago/4d/docs/RESEARCH/business-audit-2026-09.md"
      ],
      "verify": [
        "python3",
        "tools/audit_businesses.py",
        "--check"
      ]
    },
    {
      "why": "THE TOWN COMPILED FROM THE LAYER: it reads finished cards and writes the town-level record, so it follows the layer and precedes the scene. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/compile_gazetteer.py",
        "--build"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/compile_gazetteer.py",
        "--check"
      ]
    },
    {
      "why": "THE TOWN COMPILED FROM THE LAYER: it reads finished cards and writes the town-level record, so it follows the layer and precedes the scene. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/fergus_1839_street_faces.py",
        "--build"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/fergus_1839_street_faces.py",
        "--check"
      ]
    },
    {
      "why": "THE TOWN COMPILED FROM THE LAYER: it reads finished cards and writes the town-level record, so it follows the layer and precedes the scene. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/reconcile_665.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/reconcile_665.py",
        "--check"
      ]
    },
    {
      "why": "THE TOWN COMPILED FROM THE LAYER: it reads finished cards and writes the town-level record, so it follows the layer and precedes the scene. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/seat_kinzie_addition_streets.py",
        "--write"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/seat_kinzie_addition_streets.py",
        "--check"
      ]
    },
    {
      "why": "THE TOWN COMPILED FROM THE LAYER: it reads finished cards and writes the town-level record, so it follows the layer and precedes the scene. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/seat_michigan_st_tract.py",
        "--write"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/seat_michigan_st_tract.py",
        "--check"
      ]
    },
    {
      "why": "THE TOWN COMPILED FROM THE LAYER: it reads finished cards and writes the town-level record, so it follows the layer and precedes the scene. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/seat_wabansia_streets.py",
        "--write"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/seat_wabansia_streets.py",
        "--check"
      ]
    },
    {
      "why": "The liberties register, compiled from the sidecars and the town — every liberty the reconstruction takes, with its reason.",
      "command": [
        "python3",
        "tools/compile_liberties.py"
      ],
      "resolves": [
        "chicago/4d/data/liberties.json"
      ],
      "verify": [
        "python3",
        "tools/compile_liberties.py",
        "--check"
      ]
    },
    {
      "why": "The scene compiles from the residents layer, so it runs last but one.",
      "command": [
        "python3",
        "tools/compile_scene.py",
        "--all"
      ],
      "resolves": [
        "chicago/4d/data/sidecars/1835/people.json"
      ]
    },
    {
      "why": "SCENE FURNITURE DEALT FROM THE TOWN — signs, fences, plantings, infill. It reads a settled town and writes geometry nothing else reads back. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/generate_block_infill.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/generate_block_infill.py",
        "--check"
      ]
    },
    {
      "why": "SCENE FURNITURE DEALT FROM THE TOWN — signs, fences, plantings, infill. It reads a settled town and writes geometry nothing else reads back. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/generate_business_signboards.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/generate_business_signboards.py",
        "--check"
      ]
    },
    {
      "why": "SCENE FURNITURE DEALT FROM THE TOWN — signs, fences, plantings, infill. It reads a settled town and writes geometry nothing else reads back. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/generate_dooryard_pickets.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/generate_dooryard_pickets.py",
        "--check"
      ]
    },
    {
      "why": "SCENE FURNITURE DEALT FROM THE TOWN — signs, fences, plantings, infill. It reads a settled town and writes geometry nothing else reads back. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/generate_dooryard_plantings.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/generate_dooryard_plantings.py",
        "--check"
      ]
    },
    {
      "why": "SCENE FURNITURE DEALT FROM THE TOWN — signs, fences, plantings, infill. It reads a settled town and writes geometry nothing else reads back. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/generate_fort_apron.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/generate_fort_apron.py",
        "--check"
      ]
    },
    {
      "why": "SCENE FURNITURE DEALT FROM THE TOWN — signs, fences, plantings, infill. It reads a settled town and writes geometry nothing else reads back. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/generate_fort_trees.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/generate_fort_trees.py",
        "--check"
      ]
    },
    {
      "why": "SCENE FURNITURE DEALT FROM THE TOWN — signs, fences, plantings, infill. It reads a settled town and writes geometry nothing else reads back. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/generate_frontage_works.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/generate_frontage_works.py",
        "--check"
      ]
    },
    {
      "why": "SCENE FURNITURE DEALT FROM THE TOWN — signs, fences, plantings, infill. It reads a settled town and writes geometry nothing else reads back. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/generate_inferred_infill.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/generate_inferred_infill.py",
        "--check"
      ]
    },
    {
      "why": "SCENE FURNITURE DEALT FROM THE TOWN — signs, fences, plantings, infill. It reads a settled town and writes geometry nothing else reads back. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/generate_lot_building_material.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/generate_lot_building_material.py",
        "--check"
      ]
    },
    {
      "why": "SCENE FURNITURE DEALT FROM THE TOWN — signs, fences, plantings, infill. It reads a settled town and writes geometry nothing else reads back. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/generate_lot_line_fences.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/generate_lot_line_fences.py",
        "--check"
      ]
    },
    {
      "why": "SCENE FURNITURE DEALT FROM THE TOWN — signs, fences, plantings, infill. It reads a settled town and writes geometry nothing else reads back. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/generate_north_infill.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/generate_north_infill.py",
        "--check"
      ]
    },
    {
      "why": "SCENE FURNITURE DEALT FROM THE TOWN — signs, fences, plantings, infill. It reads a settled town and writes geometry nothing else reads back. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/generate_planted_rows.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/generate_planted_rows.py",
        "--check"
      ]
    },
    {
      "why": "SCENE FURNITURE DEALT FROM THE TOWN — signs, fences, plantings, infill. It reads a settled town and writes geometry nothing else reads back. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/generate_plat_lots.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/generate_plat_lots.py",
        "--check"
      ]
    },
    {
      "why": "SCENE FURNITURE DEALT FROM THE TOWN — signs, fences, plantings, infill. It reads a settled town and writes geometry nothing else reads back. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/generate_river_wharves.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/generate_river_wharves.py",
        "--check"
      ]
    },
    {
      "why": "SCENE FURNITURE DEALT FROM THE TOWN — signs, fences, plantings, infill. It reads a settled town and writes geometry nothing else reads back. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/generate_school_section_grid.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/generate_school_section_grid.py",
        "--check"
      ]
    },
    {
      "why": "SCENE FURNITURE DEALT FROM THE TOWN — signs, fences, plantings, infill. It reads a settled town and writes geometry nothing else reads back. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/generate_west_infill.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/generate_west_infill.py",
        "--check"
      ]
    },
    {
      "why": "SCENE FURNITURE DEALT FROM THE TOWN — signs, fences, plantings, infill. It reads a settled town and writes geometry nothing else reads back. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/generate_yard_goods.py"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/generate_yard_goods.py",
        "--check"
      ]
    },
    {
      "why": "The town census counts what stands and who is housed, and it is its OWN tool — compile_scene --all does not write it. Found by --prove, which is the only thing that asks whether a step writes what it claims.",
      "command": [
        "python3",
        "tools/town_census.py"
      ],
      "resolves": [
        "chicago/4d/data/town_census.json"
      ],
      "verify": [
        "python3",
        "tools/town_census.py",
        "--check"
      ]
    },
    {
      "why": "The agency relation compiles from the gazetteer, the street-face adoptions and the households, so it runs after the residents layer is rebuilt and before the audit that reads none of it. The card fetches the output, so a stale one is a relation missing from two cards rather than a number being wrong.",
      "command": [
        "python3",
        "tools/compile_agencies.py"
      ],
      "resolves": [
        "chicago/4d/data/reconstruction/1835_agencies.json"
      ],
      "verify": [
        "python3",
        "tools/compile_agencies.py",
        "--check"
      ]
    },
    {
      "why": "The final audit package re-derives from the residents layer and is the last thing downstream of all of it.",
      "command": [
        "python3",
        "tools/export_resident_audit.py",
        "--build"
      ],
      "resolves": [
        "chicago/reference/resident-research/final/audit/resident_audit_master.xlsx",
        "chicago/reference/resident-research/final/audit/resident_audit_master.csv",
        "chicago/reference/resident-research/final/audit/README.md"
      ],
      "verify": [
        "python3",
        "tools/export_resident_audit.py",
        "--check"
      ]
    },
    {
      "why": "The residence back-projection (T-0669, liberty L223). It is GENERATED by its own tool — the file's own `generated_by` says so — but it was not in this manifest, so the lap refused every PR that touched it as a REAL CONFLICT and left it for the run that owned the ticket. Measured on lap 307, 2026-09-14: #1316 was left alone for this file and nothing else. It reads the residents layer, so any two runs that touch a resident both re-derive it and it conflicts as readily as the crosswalks above.",
      "command": [
        "python3",
        "tools/back_project_residences.py",
        "--write"
      ],
      "resolves": [
        "chicago/4d/data/research/directories/residence_back_projection.json"
      ]
    },
    {
      "why": "The five DERIVED ruling registers of T-1298's remainder, and the name-on-a-roll registers of T-1297. Both read `natural_disposition`, so anything that resolves a unit on the layer drops it out of their corpus — and the ledger then refuses every unit left deferring to a ticket those registers no longer cover. They must be rewritten BEFORE the ledger reads them. Added 2026-09-18: neither was in this manifest, which is how #1448 reached a state where 190 units deferred to a done T-1298 and no `--run` could mend it.",
      "command": [
        "python3",
        "tools/spend_remainder_rulings.py"
      ],
      "resolves": [
        "chicago/4d/data/research/residents/spend_rulings.json",
        "chicago/4d/data/research/newspapers/spend_rulings.json",
        "chicago/4d/data/research/church/spend_rulings.json",
        "chicago/4d/data/research/books/spend_rulings.json",
        "chicago/4d/data/research/genealogytrails/spend_rulings.json"
      ],
      "verify": [
        "python3",
        "tools/spend_remainder_rulings.py",
        "--check"
      ]
    },
    {
      "why": "T-1297's name-on-a-roll registers, on the same footing as the remainder above and for the same reason: derived from the readings, read by the ledger, and absent from this manifest until 2026-09-18.",
      "command": [
        "python3",
        "tools/spend_name_on_a_roll_rulings.py"
      ],
      "resolves": [
        "chicago/4d/data/research/civic/spend_rulings.json",
        "chicago/4d/data/research/directories/spend_rulings.json"
      ],
      "verify": [
        "python3",
        "tools/spend_name_on_a_roll_rulings.py",
        "--check"
      ]
    },
    {
      "why": "The closed research-spend ledger (T-1143), which counts over every layer above it and so has to be last of the research steps.",
      "command": [
        "python3",
        "tools/measure_research_spend.py",
        "--ledger-build"
      ],
      "resolves": [
        "chicago/4d/data/research/research_spend_ledger.json.gz",
        "chicago/4d/docs/RESEARCH/research-spend-ledger-2026-09-15.md"
      ],
      "verify": [
        "python3",
        "tools/measure_research_spend.py",
        "--check",
        "--quiet"
      ]
    },
    {
      "why": "The borderline roster (T-1159) — every name the research read and withheld from 1835, with its reason and its re-admission class. It is DERIVED from the ledger and the resident layer and mints nobody, so anything that moves a card moves it. Added 2026-09-18 after it went red on #1422 TWICE: it landed on dev after the coverage audit ran, and the audit's first write-mode pattern only matched tools that read argv directly, so an argparse-declared --build slipped past it. MOVED BELOW THE LEDGER 2026-09-18: it was listed ABOVE the ledger it reads, so a run that changed the ledger rebuilt the roster from the OLD one and `--run` could never converge — two full passes over #1448 ended on the same four red steps, each one a reader of a ledger rebuilt after it.",
      "command": [
        "python3",
        "tools/export_borderline_roster.py",
        "--build"
      ],
      "resolves": [
        "chicago/4d/data/reconstruction/1835_borderline_roster.json",
        "chicago/4d/docs/RESEARCH/borderline-roster-2026-09.md"
      ],
      "verify": [
        "python3",
        "tools/export_borderline_roster.py",
        "--check"
      ]
    },
    {
      "why": "The closing research audit (T-1143), which re-derives from the ledger and the four layers. A gated writer that was never listed here, so a ledger rebuild left it stale and `check.sh` — not `--run` — was the only thing that noticed.",
      "command": [
        "python3",
        "tools/report_research_closing_audit.py",
        "--build"
      ],
      "resolves": [
        "chicago/4d/docs/RESEARCH/research-closing-audit-2026-09.md"
      ],
      "verify": [
        "python3",
        "tools/report_research_closing_audit.py",
        "--check"
      ]
    },
    {
      "why": "The research sign-off and its GO, read off the closing audit above. Listed here for the same reason and on the same day.",
      "command": [
        "python3",
        "tools/report_research_signoff.py",
        "--build"
      ],
      "resolves": [
        "chicago/4d/docs/RESEARCH/research-signoff-2026-09.md"
      ],
      "verify": [
        "python3",
        "tools/report_research_signoff.py",
        "--check"
      ]
    },
    {
      "why": "SHRINK ONLY, and the flag is not --write-baseline on purpose. Step 19 re-runs the synthesizer, so after a lap its outputs no longer drift and the T-0838 baseline still lists them — a ratchet that has tightened, reported as red. Shrinking can only make the gate stricter. --write-baseline regenerates and could ADD a path, recording a brand-new drift as expected; that is a judgement and stays a human's, so this step REFUSES a new drift and leaves the gate red. `resolves` is empty because there is nothing to shrink on most laps and --prove asserts a write.",
      "command": [
        "python3",
        "tools/synthesize_resident_research.py",
        "--shrink-baseline"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/synthesize_resident_research.py",
        "--drift"
      ]
    },
    {
      "why": "A REPORT OR EXPORT read off everything above it, so it is last. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/export_resident_research_package.py",
        "--index"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/export_resident_research_package.py",
        "--check"
      ]
    },
    {
      "why": "A REPORT OR EXPORT read off everything above it, so it is last. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/report_census_1840_residue.py",
        "--write"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/report_census_1840_residue.py",
        "--check"
      ]
    },
    {
      "why": "A REPORT OR EXPORT read off everything above it, so it is last. PLACED BY LAYER, NOT BY A DERIVED DEPENDENCY (T-1302 batch 2, 2026-09-18). A dependency graph was built for this — write-sets MEASURED by running all 142 tools on a clean tree, reads taken from their source — and it was rejected by its own validation: against the 54 steps already here, an order people reached by hitting real failures, the graph contradicted 127 of 349 edges (36%; 32% at the finest granularity tried). These tools overwhelmingly read and write the same shared directories, so the graph saturates and cannot tell a real dependency from co-location. Only 6 of the 89 could be placed with evidence. A clean-tree `--run` goes green in ANY order because every one of them reproduces, so greenness is not evidence here either. This position is reasoned from what the tool reads and writes; it is not measured, and the honest test is the first time a change upstream of one of these fails to propagate. WRITES, BUT DOES NOT CLAIM, ITS OUTPUTS. `resolves` is not documentation: it declares that the manifest OWNS a file, so the lap settles a conflict there by REBUILDING rather than merging. Listing what these 89 tools touch took the owned set from 72 files to 1,899 — including 1,261 household cards — and a conflict in any of them would then be rebuilt over whatever a branch had actually written. Being run by the manifest and owning your outputs are separate decisions; this is the first, and the second is not taken here. rederive.mjs's own self-test caught it, on the assertion that `hh_taylor_c.json` is a file nobody derives.",
      "command": [
        "python3",
        "tools/report_letter_list_collisions.py",
        "--write"
      ],
      "resolves": [],
      "verify": [
        "python3",
        "tools/report_letter_list_collisions.py",
        "--check"
      ]
    },
    {
      "why": "THE CLOSING SET'S OWN REPORT, AND IT GOES LAST (T-1333). docs/RESEARCH/closing-convergence-2026-09.md states the resident layer's closing numbers as deltas against a frozen baseline, so it is a function of the WHOLE tree the 143 steps above leave behind — index.json, the 1835 sidecars, the town census, the audit workbook and the standing readings T-1144 banked to this pass. Anywhere but the foot of the manifest it would report the tree half-rebuilt. `--build` measures and renders only; the rebuild of the five members themselves is the steps above, which is why this does not re-run them and cannot recurse.",
      "command": [
        "python3",
        "tools/rebuild_closing_set.py",
        "--build"
      ],
      "resolves": [
        "chicago/4d/docs/RESEARCH/closing-convergence-2026-09.md"
      ],
      "verify": [
        "python3",
        "tools/rebuild_closing_set.py",
        "--check"
      ]
    }
  ]
}
