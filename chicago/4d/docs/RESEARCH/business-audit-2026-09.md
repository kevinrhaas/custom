# The business layer, audited against the research

GENERATED by `tools/audit_businesses.py --write`; `--check` re-derives it and the gate
runs that, so a hand edit here is a red gate. T-1402, of T-1182.

This reads the 196 compiled records of `data/businesses/` back against the research they
are compiled from — `identity.json`'s rulings on who is one house and one man,
`trade_class_rulings.json`'s December 1835 census classes, and the register's own dated
brackets — and states, field by field, how well each is evidenced and what stands empty.
It makes no ruling of its own: where the evidence stops, it says where, and hands the gap
to the ticket that owns it.

**Scope: the compiled layer only.** `data/businesses/authored/` holds 8 record(s) that
a human or a reconstruction pass wrote — `biz_chicago_post_office`, `biz_cook_county_offices`, `biz_robinson_trading_house_wolf_point`, `biz_us_land_office_chicago`, `rcb_crandall_druggist`, `rcb_fb_barbers_shop`, `rcb_fb_washing_and_ironing`, `rcb_metcalf_druggist` — and they are not audited here.
They are not compiled from the register, so identity.json's merges, the register's date
fields and its anchor brackets say nothing about them and every measurement below would
be measuring the wrong thing. Their gate is `reconstruct_businesses_1835.py --check`.
The count is printed so that a layer growing past this tool cannot do it silently.

## 1. Field × grade

`rows` grades every row of a list by its own `tier`; `block` reads one `tier` off the
block; `reading` is the printed notice restated and is attested wherever it is filled.
`empty` counts RECORDS with nothing in the field — its reason is § 2.

| field | read as | attested | inferred | reconstructed | rows | empty (of 196) |
|---|---|---:|---:|---:|---:|---:|
| `name` | reading | 196 | 0 | 0 | 196 | 0 |
| `type` | reading | 191 | 0 | 0 | 191 | 5 |
| `trade` | reading | 192 | 0 | 0 | 192 | 4 |
| `occupation` | reading | 184 | 0 | 0 | 184 | 12 |
| `goods` | reading | 116 | 0 | 0 | 116 | 80 |
| `firm_styles` | reading | 28 | 0 | 0 | 28 | 168 |
| `proprietors` | rows | 111 | 0 | 0 | 111 | 85 |
| `partners` | rows | 85 | 0 | 0 | 85 | 152 |
| `staff` | rows | 0 | 0 | 0 | 0 | 196 |
| `locations` | rows | 26 | 174 | 0 | 200 | 0 |
| `dates` | block | 15 | 181 | 0 | 196 | 0 |
| `proprietor_community` | block | 0 | 17 | 94 | 111 | 85 |
| `customers` | rows | 0 | 0 | 0 | 0 | 196 |
| `sources` | reading | 196 | 0 | 0 | 196 | 0 |

## 2. Every empty field, and the policy it stands on

A field that runs empty with no policy behind it is the finding this table is for.
Every one below has one.

| field | empty | why a record may carry nothing here |
|---|---:|---|
| `type` | 5 | `not_stated` where the notice prints neither a trade nor an article to read a class off (trade_class_rulings.json's own ruling, boundary B6); never an empty list. |
| `trade` | 4 | The notice prints no trade. The class ruling records the same absence as `not_stated`. |
| `occupation` | 12 | No single occupation word the register will stand behind for this house. |
| `goods` | 80 | The notice names no article. A lawyer, a school or a physician sells no goods; a store that names none is a gap, and T-1404 works the trades from the research. |
| `firm_styles` | 168 | The house is printed under one style only, so there is no second style to keep. |
| `proprietors` | 85 | The notice names no keeper — an unsigned advertisement, or one signed by a firm style alone. A null here is a finding about the paper, not a person left out. |
| `partners` | 152 | A sole keeper, or no keeper named at all. `compile_businesses` files a lone name as a proprietor and only a firm of two or more as partners. |
| `staff` | 196 | THE PAPERS NAME OWNERS AND ALMOST NEVER A CLERK. Empty on every record is a true reading of the register: T-1183 rules the staffing model and T-1189 fills this from it. |
| `proprietor_community` | 85 | No keeper this record names has a community on their town card, so there is nothing to read a house's community off. Never inferred from a surname. |
| `customers` | 196 | The register carries no customer claim at all; T-1189 is where a workplace gets its people. |

## 3. Identity: the layer against `identity.json`

A merge is HONOURED when the merged-away spelling is not a second house or a second
person. It may still stand on `firm_styles[]` or `also_printed_as[]` of the record it
merged into — the typography is evidence and the layer keeps it there deliberately.
A refusal is honoured when the layer has not quietly folded the two together anyway.

| ruling | honoured | of |
|---|---:|---:|
| firm merges | 47 | 47 |
| proprietor merges | 12 | 12 |
| refused firm merges | 22 | 22 |
| proprietor distinctions — both men stand | 4 | 4 |

No breaches. Every merge, refusal and distinction identity.json states is the reading
the layer carries.

3 of the 47 merged-away firm spellings are kept on the merged record's `firm_styles[]`;
the rest were never printed as a standing house in this window and so have nothing to
stand on.

## 4. Dates: the register's own brackets, honoured

| reading | records | how the record is dated |
|---|---:|---|
| the paper announces the opening | 15 | `exact`, `attested` |
| no announcement, a first printing | 181 | `not_later_than`, `inferred` — the house was trading by then |
| neither | 0 | `unbounded` |
| a dissolution printed after the scene date | 1 | `closed` at the last issue that carries the house |

No record dates itself out of step with what the register prints for it.

## 5. The four houses that moved

The register brackets a change of printed anchor for four businesses: `after` is the last
issue that sets the old one, `before` the first that sets the new, and the move falls
between them. **That pair is the bracket of the MOVE and not the span of either siting**,
and it was being written onto the superseded location as `from: after, to: before` — which
dated an address to exactly the window in which it was being replaced, and carried it up
to and including the first printing of its replacement. T-1402 re-dated both rows off
their own printings; `compile_businesses.locations_for` carries the reasoning.

| record | live anchor (register's rule) | superseded / later anchor | move bracketed | live row | other row |
|---|---|---|---|---|---|
| `biz_chicago_democrat_printing_office` | Messrs. Jones & King's hardware store | the corner of South Water and Clark streets (earlier) | 1834-01-07 → 1835-05-20 | street_only 1834-01-07 → — | 1834-01-07 → 1834-01-07 |
| `biz_g_spring` | the Tremont House | the corner of Franklin and South Water streets (earlier) | 1834-11-26 → 1835-05-20 | anchored 1835-05-20 → — | 1833-12-17 → 1834-11-26 |
| `biz_matthias_mason_co` | the Tremont House | Graves' Tavern (earlier) | 1834-06-11 → 1834-09-10 | premises 1833-11-26 → — | 1833-11-26 → 1834-06-11 |
| `biz_the_chicago_democrat` | the corner of South Water and Clark streets | Jones & King's hardware store (later) | 1834-12-03 → 1835-08-05 | anchored 1833-11-26 → — | 1835-08-05 → — |

`the_chicago_democrat` is why the direction is read off `live_anchor` and never assumed:
its move to Jones & King's is first printed 1835-08-05, AFTER the scene date, so the
register keeps the corner live and the LATER siting is the second row. Read as an
“earlier” anchor like the other three, that record carried the corner twice — once live,
once as its own predecessor — and the move to Jones & King's appeared nowhere on it.

## 6. Places, and the limit on each

| kind | rows | what it means |
|---|---:|---|
| `unplaceable` | 83 | no anchor the town holds, or a prose anchor never resolved to an id |
| `street_only` | 61 | the paper names a street and no house on it |
| `premises` | 30 | matched onto a committed structure — a roof of its own |
| `anchored` | 26 | placed against a landmark the town holds; no roof of its own (T-1401) |

170 of the 200 location rows carry a `limit_reason`; every row that is not a `premises`
does. 4 records carry a plural `locations[]`: `biz_chicago_democrat_printing_office`, `biz_g_spring`, `biz_matthias_mason_co`, `biz_the_chicago_democrat` — the four of § 5.
**Every other record holds exactly one place.** The research names secondary premises for
some of these houses — a warehouse on the river, a yard, an office at a hotel, an auction
stand — and none of
them is in this layer yet, because none is in the register: the register carries ONE
anchor per business and a change of it. Raising those rows is T-1405's (for persons) and
T-1404's (for firms) with the sources in front of them; it is named here as the layer's
largest standing gap and is not fixed by an audit.

## 7. Class: `type` against the December 1835 State census

Every class on a record is a class `trade_class_rulings.json` enumerates; the crosswalk
against the census's own counts is `tools/trade_census_1835.py --check` and is not
repeated here. What this adds is the shape of the population.

| class | records | the census line |
|---|---:|---|
| `other` | 79 | (not an enumerated class) |
| `store` | 67 | forty-four stores (dry goods, hardware and groceries) |
| `lawyer` | 18 | twenty-two lawyers |
| `tavern` | 9 | eight taverns |
| `storage_and_forwarding` | 8 | four storage and forwarding houses |
| `school` | 7 | seven schools |
| `not_stated` | 5 | (not an enumerated class) |
| `steam_saw_mill` | 4 | one steam saw-mill |
| `tin_and_copper_manufactory` | 4 | two tin and copper manufactories |
| `physician` | 3 | fourteen physicians |
| `printing_office` | 3 | two printing offices |
| `book_store` | 2 | two book stores |
| `druggist` | 2 | four druggists |
| `brewery` | 1 | two breweries |
| `iron_foundry` | 1 | one iron foundry |
| `silversmith_jeweller` | 1 | two silversmiths and jewellers |

No record carries a class the ruling file does not enumerate.

`other` and `not_stated` are rulings, not gaps: `other` is a trade the enumeration does
not count (tailors, bakers, smiths, auctioneers, land agents, liveries, dentists), and
`not_stated` is a notice that prints neither a trade nor an article to read a class off.

## 8. What this audit does not settle

- **Goods on a store that names none** — T-1404, which works the in-window trades from
  the research and raises a business for each.
- **Staff** — empty on all 196 records and truthfully so. T-1183 rules the staffing model;
  T-1189 fills it.
- **Secondary premises** — § 6. T-1404 and T-1405.
- **Black-owned and Native or Métis-run houses** — T-1403.
- **The class counts against the census's own** — `tools/trade_census_1835.py`.
